<html>

<head>
    <% include partials/head %>
</head>


<body class="container">
    <div class="col-md-6.col-md-offset-3">
        <p>Hello
            <%= name %>, click bellow to see your metrics!
        </p>
        <hr>
        <button class="btn btn-success" id="show-metrics">
            Bring the metrics
        </button>
        <div id="metrics"></div>
        <hr>
    </div>

    <br>
    <hr>
    <div class="col-md-6.col-md-offset-3">
        <br>
        <form id="push-metric" method="post">
            <div class="form-group">
                <label for="form_id">ID: </label>
                <input id="form_id" type="text" name="id" />
            </div>
            <br>
            <div class="form-group">
                <label for="form_timestamp">Timestamp: </label>
                <input id="form_timestamp" type="text" name="timestamp" />
            </div>
            <br>
            <div class="form-group">
                <label for="form_value">Value: </label>
                <input id="form_value" type="text" name="value" />
            </div>
            <br>
            <button class="btn btn-primary btn-block" type="submit" value="Submit">Add metric</button>
        </form>
    </div>
    <hr>
    <br>
    <button class="btn btn-danger" href="/logout" onClick='document.location.href="/logout"'>
        Logout
    </button>
</body>

<script>
    $('#show-metrics').click((e) => {
        e.preventDefault()
        $.getJSON('/metrics/all', {}, (data) => {
            let i=0
            const content = data.map(d => {
                i++
                return `<p>id: ${i}, timestamp: ${d.timestamp}, value: ${d.value}</p>`
            })
            $("#metrics").html(content)
        })
    })

    /*  
     $('#push-metric').submit((e) => {
         const formData = new FormData(e.target);
         tm=formData.get("name").toString()
         val=formData.get("value").toString()
 
         $.post('/metrics/1', {"timestamp":"1384686660000", "value":2}, function(data){
             $.getJSON('/metrics/1',{}, (data) => {
                 $("#metrics").append(`<p>timestamp: ${data.timestamp}, value: ${data.value}</p>`)
             })
         })
         e.preventDefault();
     })
  */
    $('#push-metric').submit((e) => {
        alert("Handler for .submit() called.");
        $.post('/metrics/2', {"timestamp": "1384686660000", "value":"2"}, function (data) {
            alert("Handler for .post() called.");
        })
        e.preventDefault();
    })

</script>

</html>